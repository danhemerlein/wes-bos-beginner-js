// a flag variable - a Boolean - something that is true or false

let isDrawing = false;

let age = 233;

// booleans can be calculated
const isOfAge = age >= 21;

console.log(isDrawing);
console.log(isOfAge);

age = 100;
console.log(age);


// single = is used for assigning variables

let age2 = 100;

// === tripe equals will always check for equality in type and value
console.log(10 === 10);

// == is for checking the value only not the type
console.log('11' == 10);
  // false

